'use client'
import { Container, Selection, Form, FormTitle, CategoryContainer, CategoryBox, CategoryImage, ButtonContainer, Button  } from './styles'

export const RegisterPage = () => {
  return (
   <Container>register page</Container>
  )
}
